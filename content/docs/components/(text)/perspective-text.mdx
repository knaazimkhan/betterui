---
title: 'Perspective Text'
description: 'A 3D rotating text component with interactive perspective effects, customizable rotation speed, and smooth hover transitions.'
slug: 'components/perspective-text'
category: 'Text'
tags: ['3D Text', 'Interactive UI', 'Framer Motion', 'Hover Effects']
component: 'PerspectiveText'
---

## Overview

The `PerspectiveText` component creates an interactive 3D text effect using **Framer Motion**. It continuously rotates along the Y-axis and dynamically responds to mouse movements, providing a depth illusion.

The rotation speed and behavior can be customized for a more engaging user experience.

---

import { PerspectiveText } from '@/components/text/perspective-text'

## Preview

<PreviewComponent>
  <PerspectiveText text="3D Perspective" />
</PreviewComponent>

## Usage

```tsx
'use client'

import { PerspectiveText } from '@/components/text/perspective-text'

<PerspectiveText text="3D Perspective" />
```

## Code

<CodeSnippet componentPath="./src/components/text/perspective-text.tsx" />

## Examples

### rotationSpeed

The `rotationSpeed` is used to change the Y-axis rotation speed.

Fast Rotation

<CodeExample
  preview={ <PerspectiveText text="Fast Rotation" rotationSpeed={2} /> }
  code={`
  'use client'

  import { PerspectiveText } from '@/components/text/perspective-text'

  <PerspectiveText text="Fast Rotation" rotationSpeed={2} />
  `}
/>

Slow Rotation

<CodeExample
  preview={ <PerspectiveText text="Slow Rotation" rotationSpeed={6} /> }
  code={`
  'use client'

  import { PerspectiveText } from '@/components/text/perspective-text'

  <PerspectiveText text="Slow Rotation" rotationSpeed={6} />
  `}
/>

### reverseRotation

The `reverseRotation` is used to text rotate counterclockwise instead of clockwise.

<CodeExample
  preview={ <PerspectiveText text="Reverse Rotation" reverseRotation /> }
  code={`
  'use client'

  import { PerspectiveText } from '@/components/text/perspective-text'

  <PerspectiveText text="Reverse Rotation" reverseRotation />
  `}
/>

### className

The `className` is used to custom styles using classes.

<CodeExample
  preview={ <PerspectiveText text="Stylized Text" className="text-yellow-500" /> }
  code={`
  'use client'

  import { PerspectiveText } from '@/components/text/perspective-text'

  <PerspectiveText text="Stylized Text" className="text-yellow-500" />

  `}
/>

## Props

<AutoTypeTable
  path="./src/components/text/perspective-text.tsx"
  name="PerspectiveTextProps"
/>